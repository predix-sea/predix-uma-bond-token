name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  ci:
    uses: predix-sea/predix-platform-ci/.github/workflows/reusable-solidity-ci.yml@main
    with:
      fail-on-slither: false
    secrets: inherit
